import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const TeamTasks = () => {
  return (
    <Card>
      <Title level={2}>Team Tasks</Title>
      <p>This is a placeholder for Team Tasks component.</p>
    </Card>
  );
};

export default TeamTasks;
