import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const MyTasks = () => {
  return (
    <Card>
      <Title level={2}>My Tasks</Title>
      <p>This is a placeholder for My Tasks component.</p>
    </Card>
  );
};

export default MyTasks;
